import { IMAGE_UPLOAD_ACCEPT, formatImageMaxSize } from "@/lib/images/constants";

export const IMAGE_UPLOAD_HINT = `JPG, PNG, WebP, or GIF up to ${formatImageMaxSize()}. Images are auto-compressed to WebP on upload.`;

export { IMAGE_UPLOAD_ACCEPT };
